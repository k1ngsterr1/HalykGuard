import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  backUpperTab: {
    width: "90%",
    position: "absolute",
    top: 48,
    display: "flex",
    alignItems: "flex-start",
  },
});
